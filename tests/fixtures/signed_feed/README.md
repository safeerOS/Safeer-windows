Signed feed fixtures generated by safeer-threat-intel tests/fixtures/make_client_fixtures.py.
The signing key was discarded after generation; `public_key.json` is for tests only and must never
be added to a production trust store.
